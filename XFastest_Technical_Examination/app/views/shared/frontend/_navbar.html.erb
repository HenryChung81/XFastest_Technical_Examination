<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'XFastest', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      </ul>

      <ul class="nav nav-pills">
        <div class="d-flex">
        <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link text-dark dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown"
            aria-expanded="false">
            <%= content_tag :i, '', class: 'fas fa-crown has-text-primary' if current_user.is_blog_manager? %>
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li>
            <%= link_to 'new blog', new_blog_path, class: 'dropdown-item' %>
            </li>
            <li>
            <%= link_to 'blog list', blogs_path, class: 'dropdown-item' %>
            </li>
            <li>
            <%= link_to '個人資料', edit_user_registration_path, class: 'dropdown-item' %>
            </li>
            <hr class="dropdown-divider">
            <li>
            <%= link_to '登出', destroy_user_session_path, class: 'dropdown-item' , method: 'delete' , data:
                {confirm: '確定登出?' } %>
            </li>
          </ul>
        </li>
          <% else %>
            <li class="nav-item">
              <%= link_to '註冊', new_user_registration_path, class: 'nav-link text-dark' %>
            </li>
            <li class="nav-item">
              <%= link_to '登入' , new_user_session_path, class: 'nav-link text-dark' %>
            </li>
          <% end %>
        </div>
      </ul>
    </div>
  </div>
</nav>